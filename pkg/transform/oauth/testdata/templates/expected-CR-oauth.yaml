apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  creationTimestamp: null
  name: cluster
  namespace: openshift-config
spec:
  templates:
    error:
      name: templates-error-secret
    login:
      name: templates-login-secret
    providerSelection:
      name: templates-providerselect-secret
  tokenConfig:
    accessTokenMaxAgeSeconds: 0
